# Markdown Sublime Text snippets

Markdown snippets for Sublime Text. Add the repo in Sublime settings [ctrl + shift + p] > "Add repository", and then install package via package-manager [ctrl + shift + p] > Install package.
